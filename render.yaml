services:
  - type: web
    name: mysignvoice
    runtime: docker
    plan: free
    region: singapore
    branch: codex/render-deployment
    dockerfilePath: ./Dockerfile
    healthCheckPath: /api/health
